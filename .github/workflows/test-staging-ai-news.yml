name: Test Staging AI News Generation

on:
  workflow_dispatch:  # 手動実行のみ
    inputs:
      test_description:
        description: 'テストの説明'
        required: false
        default: 'ステージング環境でのOpenAI APIテスト'

permissions:
  contents: write

jobs:
  test-staging-ai-news:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Install dependencies
      run: npm install
      
    - name: Generate AI news with OpenAI API
      run: node scripts/generate-ai-news-openai.js
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        
    - name: Build Next.js application
      run: npm run build
      
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ap-northeast-1
        
    - name: Deploy to staging S3
      run: |
        aws s3 sync out/ s3://${{ secrets.S3_BUCKET_STAGING }} --delete
        
    - name: Verify deployment
      run: |
        echo "🎉 ステージング環境テスト完了!"
        echo "📊 生成された記事を確認してください:"
        echo "🌐 ステージング環境: http://${{ secrets.S3_BUCKET_STAGING }}.s3-website-ap-northeast-1.amazonaws.com"
        echo "📰 ニュースページ: http://${{ secrets.S3_BUCKET_STAGING }}.s3-website-ap-northeast-1.amazonaws.com/news"
        
    - name: List generated files
      run: |
        echo "生成されたファイル:"
        find posts/ -name "*ai-news-openai*" -type f | head -10
        
    - name: Commit generated articles (optional)
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action - Staging Test"
        git add posts/
        if git diff --staged --quiet; then
          echo "変更なし"
        else
          git commit -m "🧪 Staging test: Add AI news articles generated via OpenAI API"
          git push origin main
        fi